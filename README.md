# jargs
minimalist node command line arg parser

usage:
```javascript
const {
  n:name = 'jargs',
  v:version = '1.0'
} = require('./jargs.js')

console.log(`My name is ${name} and my version is ${version}`)
```
